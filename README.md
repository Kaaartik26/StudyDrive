# StudyDrive
A lightweight class file-sharing web app built with Flask that works without a database. Admins can create/delete folders and manage files, while users can upload and download files inside predefined folders. Files are stored directly in the server filesystem, and metadata is tracked using JSON.
